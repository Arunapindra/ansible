- name: installing nginx
  hosts: web
  become: yes
  tasks:
  - name: Install nginx
    ansible.builtin.package: # heterogenous module that can work for all distribution
      name: nginx
      state: present
